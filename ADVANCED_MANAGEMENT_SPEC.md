# מסך ניהול מתקדם — אפיון

דגל: `advanced_management` · מסלול: `/events/:id/manage` · היקף: משחק אחד.

**סטטוס: ממומש.** המסמך מתאר את מה שנבנה. מה שנותר לך הוא ליצור את שורת
הדגל בפאנל האדמין — עד אז המסלול קיים ומפנה חזרה לדף הבקרה, וזה כל מה
שמישהו יראה.

---

## 1. מה הבעיה

היום כל מה שהמנהל יודע על המשתתפים שלו מפוזר בין שלושה טאבים במודל
(`src/components/manage/ManageModal.tsx`), שכל אחד מהם עונה על שאלה אחת בזמן
אמת: מי סרק, מי קיבל פרס, מי זכה בהגרלה. זה בדיוק הנכון תוך כדי אירוע —
מסתכלים, סוגרים, חוזרים לדף הבקרה.

מה שאין שם זו **השורה של המשתתף**. אין מקום אחד שאומר "דנה כהן, קבוצה א,
050-1234567, 340 נקודות, 12 סריקות, האחרונה ב-14:32, זכתה בשובר ובהגרלת
האופניים" — ואין דרך להוציא את זה החוצה. מנהל תוכנית שרוצה לשלוח לרכזת
טבלה, להעלות למערכת אחרת, או פשוט לשמור את התוצאות אחרי שהמשחק נגמר, נשאר
עם צילומי מסך.

זה מה שהמסך הזה נותן: טבלה אחת מלאה, וכפתור ייצוא.

## 2. הדגל

```
key           advanced_management
label         ניהול מתקדם
description   מסך ניהול מלא בדף נפרד: טבלת משתתפים עם כל הנתונים, סינון וייצוא לאקסל.
default_plans (ריק, או organizations — לפי מה שנמכר)
```

נוצר **ביד בפאנל האדמין**, לא במיגרציה.

בקוד — קובץ בתבנית של `src/lib/roster/importCsvFlag.ts`:

```ts
// src/lib/manage/advancedManagementFlag.ts
export const ADVANCED_MANAGEMENT_FLAG = 'advanced_management'
export function useAdvancedManagement(): boolean {
  return useFeatureFlag(ADVANCED_MANAGEMENT_FLAG)
}
```

עד שהשורה קיימת בקטלוג, המסלול, הקישור אליו והכל שבתוכו פשוט לא קיימים —
זו התנהגות ברירת המחדל של `FeatureGate`, ולא צריך לעשות בשבילה כלום.

## 3. איפה זה יושב

### המסלול

```tsx
// src/App.tsx, ליד /events/:id/control
<Route path="/events/:id/manage" element={
  <ProtectedRoute><AppShell atmosphere="control"><EventManagePage /></AppShell></ProtectedRoute>
} />
<Route path="/events/:id/manage/:tab" element={ ... אותו דבר ... } />
```

`src/pages/EventManagePage.tsx` בנוי בדיוק כמו `EventControlCenter.tsx`:
טוען את האירוע, `FullPageLoader` בינתיים, עוטף ב-`EventFeaturesProvider`
ומוסר ל-`AdvancedManageScreen`.

**השער.** הדגל נקרא רק בתוך הפרוביידר, אז הבדיקה יושבת בקומפוננטה הפנימית:

| מצב | מה קורה |
|---|---|
| `loading` | `FullPageLoader` — לא מפנים לפני שיודעים |
| דגל כבוי | `<Navigate to={`/events/${id}/control`} replace />` |
| דגל דלוק | המסך |

מפנים ולא נועלים, מאותה סיבה שכתובה ב-`importCsvFlag.ts`: משחק בלי הפיצ'ר
לא אמור לראות דלת נעולה לחדר שלא נמכר לו. מי שהגיע לכתובת ידנית נוחת בדף
הבקרה, לא בשגיאה.

### הכניסה

כפתור "ניהול" בדף הבקרה — אותו כפתור, אותו מקום. **עם הדגל הוא מנווט לדף
המלא; בלעדיו הוא פותח את הפופאפ כמו תמיד.**

זו לא הבחירה הראשונה שעשיתי. תחילה הפופאפ נשאר הכניסה לכולם, והדגל רק הוסיף
בתוכו קישור "מסך ניהול מלא". זה נכשל ברגע שראו אותו: מי שקנה מסך ניהול מלא
לחץ "ניהול" והגיע לפופאפ עם קישור קטן בפינה. שתי דלתות באותו שם, כשאחת מהן
נמצאת בתוך השנייה, גרועות מדלת אחת שמובילה למה שלמשחק באמת יש.

הפופאפ עצמו נשאר בקוד ולא השתנה, משתי סיבות: הוא עדיין כל הניהול של כל
משחק בלי הדגל, והוא הגרסה היחידה שמשחק אופליני מיוצא יכול להריץ
(`src/offline/screens/OfflineHub.tsx:60`) — בקובץ הזה אין מסלולים לנווט
אליהם.

הניווט רגיל ולא `target="_blank"`: לדף יש כתובת משלו שאפשר לשמור ולשלוח,
וקישור "חזרה למשחק" מחזיר לדף הבקרה.

הערה אחת על תזמון: `useAdvancedManagement()` מחזיר `false` כל עוד קטלוג
הדגלים בדרך, כמו כל גייט באפליקציה. לחיצה על "ניהול" בשבריר השנייה הראשון
של טעינה קרה תפתח את הפופאפ. הקטלוג נשמר במטמון לכל הסשן, אז זה חלון של
ביקור אחד — אם זה יפריע בפועל, הפתרון הוא להמתין ל-`loading` בדף הבקרה עצמו
כמו שעושה `EventManagePage`.

## 4. המסך

```
┌──────────────────────────────────────────────────────────────────┐
│ ← חזרה למשחק                                                     │
│ ניהול · ליל הפעילות                                              │
│ כל נתוני המשתתפים של המשחק, לצפייה ולייצוא                        │
│                                                                   │
│ [ משתתפים ] [ סריקות ] [ פרסים ] [ הגרלות ]                      │
│ ───────────                                                       │
│ [🔍 חיפוש...]      [קבוצה ▾] [סטטוס ▾] [ניקוי מסננים] [ייצוא ▾]  │
│                                                                   │
│ 128 משתתפים · 96 שיחקו · 1,240 סריקות · 34,500 נקודות            │
│                                                                   │
│  מקום  שם          קבוצה   טלפון        נק׳   סריקות  אחרונה     │
│  ───────────────────────────────────────────────────────────────  │
│   1    דנה כהן      א      050-1234567   340    12     14:32     │
│ ▾ 2    יוסי לוי     ב      052-7654321   280     9     12.7·14:18│
│      ┌──────────────────────────────────────────────────┐        │
│      │ קבוצה ב · אימייל … · פרסים … · זכיות בהגרלה …    │        │
│      │ [ 9 הסריקות של יוסי לוי ]                        │        │
│      └──────────────────────────────────────────────────┘        │
│   —    אבי ישראלי   ב      054-9876543     0     0        —      │
└──────────────────────────────────────────────────────────────────┘
```

הייצוא יושב בשורת הכלים של טאב המשתתפים ולא בכותרת הדף, כי הוא מייצא את
הטבלה הזאת. כפתור בכותרת היה מבטיח משהו כלל-מסכי שהוא לא עושה.

טאבים ככתובת: `/events/:id/manage/participants` (ברירת מחדל), `/scans`,
`/rewards`, `/lottery`. בניגוד למודל — שהוא "דרך אגב, לא מקום להצביע עליו"
— לדף הזה כן מצביעים, אז הטאב יושב ב-URL.

**שלושת הטאבים האחרונים הם הקומפוננטות הקיימות, ללא שינוי.** `ScansTab`,
`RewardsTab` ו-`LotteryTab` כבר מקבלות `eventId` ועומדות בפני עצמן. הן
נכנסות לדף כמו שהן; אם לא היה טאב חדש, הדף היה רק מודל מתוח.

### טאב המשתתפים — הדבר החדש

שורה למשתתף, וכל מה שידוע עליו באותה שורה.

| עמודה | מקור |
|---|---|
| שם פרטי / משפחה | `participants.first_name`, `last_name` (083) |
| קבוצה | `participant_groups` → `groups.name` |
| טלפון | `participants.phone` (081) |
| נקודות | סכום `point_transactions.points` |
| מקום | הדירוג לפי נקודות |
| סריקות | מספר השורות ב-`point_transactions` |
| סריקה אחרונה | `max(point_transactions.created_at)` |
| פרסים | `participant_rewards` → `rewards.name` |
| זכיות בהגרלה | `lottery_draws.winner_participant_id` |
| נוסף בתאריך | `participants.created_at` |

**עמודה שכולה ריקה לא מוצגת.** משחק בלי טלפונים לא רואה עמודת טלפון, משחק
בלי הגרלות לא רואה עמודת הגרלות. זה ההבדל בין מסך נוח לבין ים של מקפים,
וזה גם מייתר תלות בדגלים אחרים: לא צריך לשאול אם למשחק יש `sms_notifications`
כדי להחליט על עמודת הטלפון — שואלים אם יש בה משהו.

**אין אימייל.** בגרסה הראשונה הייתה עמודת אימייל, לפי מיגרציה 002. מיגרציה
005 מוחקת את העמודה הזאת מ-`participants`, וזה הפיל את כל הקריאה — כולל
מסלול הנפילה, שגם הוא ביקש `email`. משתתף מגיעים אליו בטלפון, לא בדואר.

**אין מזהה.** `external_id` הוא הקוד שעל הברקוד; הוא הכרחי לסריקה ולא אומר
כלום למי שקורא רשימה. הוא לא נקרא, לא מוצג, ולא ניתן לחיפוש כאן.

**פעולות בטבלה**

- חיפוש חופשי: שם, טלפון, אימייל, מזהה, שם קבוצה.
- מסננים: קבוצה (כולל "ללא קבוצה") · שיחקו / לא שיחקו / זכו בפרס / זכו בהגרלה.
- מיון בלחיצה על כותרת עמודה. תא ריק צונח לתחתית בשני הכיוונים — מיון עולה
  לפי "סריקה אחרונה" אמור להעלות את הסריקה המוקדמת ביותר, לא את מאה האנשים
  שלא סרקו בכלל.
- מקום בטבלה: תיקו מתחלק באותו מקום והבא מדלג. מי שלא צבר נקודות הוא בלי
  מקום, לא במקום האחרון עם כל מי שעוד לא התחיל.
- הרחבת שורה → מה שלא נכנס לעמודה (אימייל, מזהה, רשימת הפרסים והזכיות), עם
  כפתור שקופץ לטאב הסריקות כשהחיפוש כבר מכיל את השם.

הסריקות עצמן לא משוכפלות לכאן: הן טאב משלהן, עם המחיקה שכבר קיימת בו דרך
`DeleteScanDialog` וה-RPC של 074. שורה שמקשרת לשם עדיפה על עותק שני של
היומן שיצטרך להישאר מסונכרן איתו.

**המסך קורא, לא עורך.** מחיקת סריקה היא היוצאת מן הכלל היחידה, והיא כבר
קיימת היום במודל. עריכת משתתפים, הוספה ומחיקה שייכות לאשף ולייבוא הרשימה,
ולא נכנסות לכאן.

## 5. הייצוא

`src/lib/spreadsheet` כבר יודע לבנות xlsx עם RTL ורוחבי עמודות, ו-CSV עם
BOM. אין צורך בספרייה חדשה.

כפתור אחד עם תפריט:

| פריט | מה יורד |
|---|---|
| **הטבלה שעל המסך · אקסל** | בדיוק העמודות, השורות והמיון שרואים אחרי החיפוש והמסננים |
| **הטבלה שעל המסך · CSV** | אותו דבר, לתוכנות שמסרבות ל-xlsx — אותו נימוק שכבר כתוב ב-`rosterTemplate.ts` |
| **חוברת מלאה** | גיליון לכל דבר: סיכום · משתתפים · סריקות · פרסים · הגרלות |

הכלל שמנחה את שני הראשונים: **מה שרואים זה מה שיורד.** מנהל שסינן לקבוצה א
וייצא, מצפה לקבוצה א. ייצוא שמתעלם מהמסננים הוא באג, לא פיצ'ר.

החוברת המלאה היא בכוונה לא מה שעל המסך — היא כל מה שלמשחק יש, והיא אומרת
זאת בכך שהיא פריט אחר בתפריט. שני דברים נוספים שמותר לה בגלל זה:

- גיליון המשתתפים מפצל את השם לשם פרטי ומשפחה כשיש מה לפצל. קובץ הוא המקום
  שבו נעשה מיזוג דואר או רשימת כיתה, וזו העמודה היחידה שמרוויחה מהפיצול.
  המסך ממשיך להראות שם אחד, כי זה מה שכל שאר האפליקציה מראה.
- גיליון הגרלות מופיע רק אם רצה הגרלה. גיליון ריק הוא שאלה, לא תשובה —
  אותו כלל של עמודה ריקה.
- החוברת קוראת את הנתונים שלה רק כשלוחצים עליה. רוב הביקורים לא מייצאים,
  ויומן הסריקות הוא הדבר הגדול ביותר שלמשחק יש.

שמות הקבצים: `{שם המשחק}-משתתפים-{YYYY-MM-DD}.xlsx` ו-`…-נתונים-….xlsx`.

גיליון הסיכום: המשחק, מתי הופק, ואז משתתפים / שיחקו / סריקות / נקודות /
פרסים שחולקו / זכיות בהגרלה — ואחריהם אותם מספרים בפילוח לפי קבוצה. הכל
נגזר מהגיליונות האחרים בכוונה; מספר שאי אפשר לבדוק מול הם מספר שאין בו טעם.

### מה נבנה בשביל זה

`buildXlsx` ידע לכתוב **גיליון אחד בלבד** — `sheet1.xml` קבוע בשלושה מקומות
בתוך ה-zip. הוא הוחלף ב-`buildXlsxWorkbook(sheets)`, ש-`buildXlsx` עצמו
קורא לו עכשיו עם גיליון אחד, כך שהייבוא והתבנית לא ידעו שקרה משהו. נוסף
לזה ניקוי שמות גיליונות: אקסל פוסל `: \ / ? * [ ]` ומעל 31 תווים, ופוסל
בכך שהוא מסרב לפתוח את הקובץ כולו — לא בכך שהוא מקצר.

### פרטיות

הקובץ יוצא מהמערכת ובתוכו טלפונים ואימיילים. הייצוא מוגבל למי שכבר רשאי
לקרוא את המשתתפים לפי ה-RLS — בעל המשחק ומשתפי הפעולה שלו. אין כאן נתיב
חדש לנתונים, רק פורמט חדש.

## 6. איסוף הנתונים

הוק אחד, `useEventParticipantsReport(eventId)`, שמחזיר את השורות המוכנות.
ארבע שאילתות מקבילות (משתתפים+קבוצות, תנועות נקודות, פרסים, הגרלות)
ואיחוד בזיכרון — לא JOIN אחד גדול, כי כל אחת מהן היא כבר תבנית שקיימת
בהוקים הסמוכים.

**מסד נתונים שנשאר מאחור לא שובר את המסך.** אין 081/083 — הקריאה חוזרת בלי
`phone` ו-`first_name`/`last_name`, והעמודות פשוט לא מופיעות. אין 080 —
אין טבלת הגרלות, וזו עובדה על המשחק ולא קריאה שנכשלה. שתי ההבחנות
(`isMissingColumn` / `isMissingTable`) הוצאו מ-`useEventLotteryRuns`
ל-`src/lib/supabaseErrors.ts` כדי שגם זה וגם זה יקראו אותן.

**עימוד.** PostgREST חותך תשובה ב-1000 שורות ואומר זאת בכך שהוא מחזיר פחות.
לכל מסך אחר באפליקציה זה בסדר, כי רשימה על מסך גוללים ולא סופרים; לייצוא
זה לא בסדר, כי קובץ שנעצר בשקט אחרי אלף סריקות גרוע מקובץ שנכשל — אף אחד
לא יבחין. לכן `fetchAllRows` (`src/lib/supabasePaging.ts`) קורא חלון אחרי
חלון עד שמגיע עמוד קצר.

מעבר לעימוד אין שרת בתמונה: נטען הכל, מסננים ומיינים בזיכרון. אם משחק יחצה
כמה אלפי משתתפים כדאי לחזור לזה עם וירטואליזציה של הטבלה.

## 7. מה נגעתי בו

חדש:

| קובץ | תפקיד |
|---|---|
| `src/lib/manage/advancedManagementFlag.ts` | הדגל והכתובת |
| `src/lib/manage/participantsReport.ts` | העמודות, המסננים, המיון, הדירוג — הכל טהור |
| `src/lib/manage/participantsExport.ts` | שלושת הייצואים |
| `src/lib/manage/participantsReport.test.ts` | 12 בדיקות · `npm run test:manage` |
| `src/hooks/useEventParticipantsReport.ts` | הקריאה |
| `src/components/manage/ParticipantsTab.tsx` | הטבלה |
| `src/components/manage/AdvancedManageScreen.tsx` | הדף וארבעת הטאבים |
| `src/pages/EventManagePage.tsx` | המסלול והשער |
| `src/lib/supabaseErrors.ts`, `src/lib/supabasePaging.ts` | משותפים |

קיים ושונה:

- `src/lib/spreadsheet/xlsx.ts` + `index.ts` — חוברת רב-גיליונית
- `src/components/manage/ManageModal.tsx` — הקישור המגודר
- `src/components/manage/ScansTab.tsx` — `initialQuery`, ופורמט הזמן שהיה
  משוכפל שם עבר ל-`participantsReport`
- `src/hooks/useEventLotteryRuns.ts` — שתי הבדיקות שיצאו ממנו
- `src/App.tsx` — שני המסלולים

## 8. פתוח

**מה קורה למודל בטווח הארוך?** כרגע שניהם חיים זה לצד זה, והמודל מנצח
באופליין. אם המסך המלא יתפוס, שווה לשקול שהמודל יישאר רק לאופליין.
