# שאלות טריוויה — אפיון

דגל: `trivia_tasks` · היקף: משחק אחד · מיגרציה: 088.

**סטטוס: ממומש.** כל שבעת הצעדים בסעיף 15 נבנו. מה שנותר הוא להריץ את
`supabase/APPLY_TRIVIA_TASKS.sql` וליצור את שורת הדגל בפאנל האדמין — עד אז
הכל כבוי ושום התנהגות קיימת לא השתנתה.

---

## 1. מה זה

היום כל משימה היא כרטיס אחד: סורקים אותו — מקבלים נקודות. אין במשחק שום דבר
שהמשתתף יכול **לטעות** בו.

שאלת טריוויה היא משימה שמדפיסה **שלושה כרטיסים** — כרטיס לכל תשובה
אפשרית. המשתתף קורא את השאלה, בוחר את הכרטיס שלדעתו נכון, וסורק אותו. רק
אחד משלושת הכרטיסים מזכה בנקודות; השניים האחרים שווים אפס. יש ניסיון אחד,
כי סריקה שגויה נרשמת בדיוק כמו סריקה נכונה ושורפת אותו.

מבחינת המנהל זו עדיין **משימה אחת** ברשימה, עם ניקוד אחד, שיוך לקבוצות אחד
ומגבלה אחת. שלושת הכרטיסים הם פרט של ההדפסה, לא שלוש משימות.

## 2. הדגל

```
key           trivia_tasks
label         שאלות טריוויה
description   משימה שמדפיסה 3 כרטיסי תשובה - רק הנכון מזכה בנקודות.
default_plans (ריק - נמכר לפי משחק)
```

נוצר **ביד בפאנל האדמין**, לא במיגרציה.

```ts
// src/lib/tasks/triviaTasksFlag.ts  - בתבנית של advancedManagementFlag.ts
export const TRIVIA_TASKS_FLAG = 'trivia_tasks'
export function useTriviaTasks(): boolean {
  return useFeatureFlag(TRIVIA_TASKS_FLAG)
}
```

**הדגל שולט על היצירה בלבד, לא על הסריקה.** הכפתור בויזארד, המחבר, ותצוגת
השאלה בכרטיסים — כולם מאחורי הדגל. אבל `useScoreSubmit` פותר קוד תשובה
תמיד, בלי לשאול על הדגל: משחק שהדפיס כרטיסי טריוויה ואז הדגל הוסר ממנו
חייב להמשיך לסרוק אותם כמו שצריך. דגל שנסגר עוצר יצירה של שאלות חדשות,
הוא לא הופך ערימת כרטיסים מודפסת לפסולה.

## 3. מודל הנתונים — מיגרציה 088

### `actions.kind`

```sql
ALTER TABLE actions
  ADD COLUMN IF NOT EXISTS kind TEXT NOT NULL DEFAULT 'standard'
  CHECK (kind IN ('standard', 'trivia'));
```

כל מה שקיים היום נשאר `standard` וממשיך בדיוק כמו שהוא. אף שאילתה קיימת לא
משתנה — כולן קוראות `select('*')` או רשימת עמודות מפורשת, ושתיהן שורדות
עמודה חדשה.

בטיפוסים `kind` הוא **אופציונלי** (`kind?: ActionKind`), ו"חסר" פירושו
`standard`. שתי סיבות: מסד שעדיין לא הריץ 088, ושאילתות שמפרטות עמודות
במקום `select('*')` — למשל ההוספה המהירה ב-`InlineAddAction`. להוסיף שם
`kind` לרשימה היה שובר הוספת משימה רגילה בכל מסד שהמיגרציה לא רצה בו.
הקריאה עוברת דרך `isTriviaAction()`, אף פעם לא השוואה ישירה לשדה.

### `action_options` — התשובות

```sql
CREATE TABLE action_options (
  id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  action_id  UUID NOT NULL REFERENCES actions(id) ON DELETE CASCADE,
  event_id   UUID NOT NULL REFERENCES events(id) ON DELETE CASCADE,
  code       TEXT NOT NULL,
  label      TEXT NOT NULL CHECK (length(btrim(label)) > 0),
  is_correct BOOLEAN NOT NULL DEFAULT false,
  sort_order INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE UNIQUE INDEX idx_action_options_event_code ON action_options(event_id, code);
CREATE INDEX idx_action_options_action ON action_options(action_id);
```

`event_id` יושב כאן גם כשאפשר להגיע אליו דרך `action_id`, משתי סיבות:
המדיניות ב-RLS נכתבת בלי join, והפותר בסריקה שואל "הקוד הזה, במשחק הזה"
בשאילתה אחת.

**הקודים.** טריגר `generate_action_option_code()` נותן `A-1003-1`,
`A-1003-2`, `A-1003-3` לפי קוד המשימה. הם לא מתנגשים עם קודי משימות —
`generate_action_code()` (מיגרציה 005) מייצר רק `^A-[0-9]+$`, והסיומת
פוסלת התאמה. הם גם לא מכילים `*`, המפריד של פורמט code128
(`src/lib/cardPayload.ts`), אז המטען הקומפקטי נשאר חד-משמעי.

**הקוד לא מסגיר את התשובה.** במשחק `code128` הקוד מודפס כטקסט מתחת לברקוד
(כדי שאפשר יהיה להקליד אותו ידנית) — לכן `-1/-2/-3` מוגרלים לתשובות בסדר
אקראי בשמירה, ואין שום קשר בין המספר לנכונות. אין "קוד המשימה = התשובה
הנכונה": גם התשובה הנכונה היא שורה ב-`action_options`, ו-`actions.code` של
שאלת טריוויה פשוט לא מודפס על שום כרטיס.

**אילוץ תקינות.** *לכל היותר* תשובה נכונה אחת נאכף באינדקס ייחודי חלקי —
`UNIQUE (action_id) WHERE is_correct`. זול, בלי טריגר, ובלי לחכות ל-commit.

*לפחות* אחת לא ניתן לאכוף ברמת השורה: שאלה נבנית שורה-שורה ויש לה אפס
תשובות נכונות עד ההוספה השנייה. זה תפקיד הוולידציה במחבר, ורשת הביטחון היא
האזהרה על שורת המשימה (סעיף 9) — שאלה בלי תשובה נכונה לא מודפסת, ואומרים
את זה במקום.

הערה למי שיכתוב את העריכה: העברת הנכונה משורה א׳ לשורה ב׳ חייבת לנקות
קודם את א׳ ורק אז לסמן את ב׳. שתיהן יחד נופלות על האינדקס.

### RLS

אותו דפוס בדיוק כמו `actions` במיגרציה 003 — קריאה, יצירה, עדכון ומחיקה
לבעל המשחק:

```sql
USING (event_id IN (SELECT id FROM events WHERE owner_admin_id = auth.uid()))
```

הסריקה רצה תחת ה-session של הבעלים (`useScoreSubmit` משתמש ב-`user!.id`),
אז אין צורך במדיניות נוספת.

### קובץ APPLY

`supabase/APPLY_TRIVIA_TASKS.sql` — כמו כל מיגרציה אחרת שצריכה להיות מורצת
ביד ב-SQL editor. אידמפוטנטי.

## 4. הויזארד, שלב 4 — הלב של הפיצ'ר

`src/components/wizard/StepTasks.tsx` → `src/components/actions/ActionList.tsx`

### נקודת הכניסה

היום מתחת לרשימה יש שורת הוספה אחת (`InlineAddAction`): שם + נקודות +
"הוסף פעילות". עם הדגל דלוק מתווספת לידה **פעולה משנית אחת**, לא בורר מצב:

```
┌──────────────────────────────────────────────────────────┐
│  שם המשימה - לדוגמה: פתרון חידה...      [10 נק׳]  [הוסף] │
└──────────────────────────────────────────────────────────┘
        ❓ או הוסיפו שאלת טריוויה - 3 תשובות, אחת מזכה
```

הוספת משימה רגילה נשארת מהירה בדיוק כמו שהיא. הטריוויה היא לחיצה אחת
נוספת, והמשפט שלידה עושה את כל ההסבר שהלקוח צריך כדי להבין למה ללחוץ.

בלי הדגל — השורה הזו לא קיימת. לא נעולה, לא מוצגת.

### המחבר — `TriviaComposerModal`

מודל (`src/components/ui/Modal.tsx`), לא טופס inline: יש כאן מספיק שדות
כדי שהוא יהיה מסך בפני עצמו, והלקוח צריך לראות את התוצאה לפני שהוא שומר.

```
┌────────────────────────────────────────────────────────────────┐
│  שאלת טריוויה                                             ✕    │
├────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 🖨  יודפסו 3 כרטיסים - כרטיס לכל תשובה. המשתתף סורק את   │  │
│  │    הכרטיס שנראה לו נכון. רק התשובה הנכונה מזכה בנקודות,  │  │
│  │    ולכל משתתף יש ניסיון אחד.                              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                │
│  השאלה                                                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ באיזו שנה הוקם היישוב?                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                │
│  התשובות · סמנו את הנכונה                                      │
│  ○  ┌────────────────────────────────────────────┐            │
│     │ 1948                                        │            │
│  ●  ┌────────────────────────────────────────────┐   ← נכונה  │
│     │ 1952                                        │            │
│  ○  ┌────────────────────────────────────────────┐            │
│     │ 1967                                        │            │
│                                                                │
│  נקודות                  [ 20 ]     תשובה שגויה: 0 נקודות     │
│                                                                │
│  ─────────────  כך זה יודפס  ─────────────                     │
│   ┌────────┐  ┌────────┐  ┌────────┐                          │
│   │ ▪▪▪ QR │  │ ▪▪▪ QR │  │ ▪▪▪ QR │   הכרטיסים יודפסו       │
│   │  1948  │  │  1952  │  │  1967  │   בסדר אקראי, בלי        │
│   │ שאלה…  │  │ שאלה…  │  │ שאלה…  │   סימון על הנכון         │
│   └────────┘  └────────┘  └────────┘                          │
│                                                                │
│                              [ ביטול ]   [ שמירת השאלה ]       │
└────────────────────────────────────────────────────────────────┘
```

שלושה דברים כאן עושים את העבודה הידידותית, וכדאי לא לוותר על אף אחד:

1. **פס ההסבר למעלה** — שלושה משפטים שאומרים בדיוק מה יקרה במציאות, לא מה
   המערכת עושה.
2. **התצוגה המקדימה של הכרטיסים** — הלקוח רואה את מה שיצא מהמדפסת לפני
   שהוא שומר, כולל זה שהנכונה לא מסומנת שם.
3. **"תשובה שגויה: 0 נקודות" כטקסט קבוע** — לא שדה. בגרסה הראשונה זה לא
   ניתן לעריכה, וזה עדיף שייכתב מפורש מאשר שיישאר לניחוש.

**ולידציה לפני שמירה:** שאלה לא ריקה · שלוש תשובות לא ריקות · אין שתי
תשובות זהות · בדיוק אחת מסומנת נכונה. כל הודעה יושבת ליד השדה שלה.

**מה נשמר:** שורת `actions` אחת עם `kind='trivia'`, `points` = הניקוד
לתשובה נכונה, `max_completions = 1` (סעיף 7), ושלוש שורות `action_options`
עם `sort_order` מוגרל.

### השורה ברשימה — `ActionRow`

שאלת טריוויה מוצגת כשורה אחת, כמו כל משימה, עם:

- צ׳יפ `❓ טריוויה` ליד השם.
- מתחת לשם — שלוש התשובות כטקסט קטן, עם `✓` על הנכונה. **רק כאן**; לא
  בכרטיס, לא בקיוסק, לא בהדפסה.
- ניקוד, קבוצות ומחיקה — בדיוק הרכיבים הקיימים.
- עריכה של השם/הניקוד inline נשארת. עריכת התשובות פותחת את המחבר.
- בורר המגבלה (`TaskLimitSelect`) מוצג נעול על "פעם אחת", עם הסבר
  בטולטיפ: "לשאלת טריוויה יש ניסיון אחד - אחרת אפשר לסרוק את שלושת
  הכרטיסים".

### ספירות ומגבלות תוכנית

שאלת טריוויה היא **שורת `actions` אחת**, ולכן נספרת כמשימה אחת:
`counts.tasks`, מגבלת המשימות של התוכנית, `computeWizardState`, המוכנות —
כולם ממשיכים לעבוד בלי שינוי. זה בדיוק מה שהמודל של סעיף 3 קונה.

## 5. שלב הכרטיסים וההדפסה

`src/components/wizard/StepCards.tsx` · `src/lib/cardCounts.ts` ·
`src/components/qr-cards/QrCardGenerator.tsx`

זה המקום שבו "3 ברקודים" באמת עולה כסף — נייר.

### הספירה

`computeCardCounts` היום סופרת כרטיס אחד למשימה. עכשיו כל משימה שוקלת
`optionCount` (1 לרגילה, 3 לטריוויה):

- **split**: `participants + Σ optionCount` — שאלת טריוויה מוסיפה 3 כרטיסי
  משימה במקום 1.
- **combined**: לכל משתתף, סכום ה-`optionCount` של המשימות הרלוונטיות לו —
  כלומר **3 כרטיסים לכל משתתף לכל שאלה**. משחק של 60 משתתפים עם 4 שאלות
  טריוויה מדפיס 720 כרטיסים רק עליהן.

זה חייב להיאמר ללקוח בשלב הכרטיסים, לא להתגלות במדפסת. `DeckDetail` מקבל
שורה נוספת כשיש טריוויה במשחק:

> `12 כרטיסי משתתף · 9 כרטיסי משימה (2 שאלות טריוויה = 6 כרטיסים)`

ובמצב combined, אם המספר חוצה סף (נניח 300), פס `Alert` רגוע:
"שאלות הטריוויה מכפילות פי 3 את מספר הכרטיסים למשתתף. במצב 'כרטיס משתתף +
כרטיס משימה' יודפסו 9 כרטיסים בסך הכל במקום 720." — כלומר להציע `split`,
לא לחסום.

### הכרטיס עצמו

`QrCard` מקבל וריאנט טריוויה:

- **eyebrow**: `שאלת טריוויה` במקום `כרטיס משימה`.
- **כותרת**: **התשובה** (`option.label`) — זה מה שהמשתתף בוחר לפיו.
- **מתחת**: השאלה, בפונט קטן ומעומעם, על כל שלושת הכרטיסים.
- **footer**: **בלי שורת הנקודות.** להדפיס `+20 נקודות` על כרטיס של תשובה
  שגויה זה שקר על הנייר. במקומה — `סרקו את התשובה שנראית לכם נכונה`.
- הקוד מתחת לברקוד (במשחקי `code128`) נשאר כמו שהוא ולא מסגיר כלום.

### סידור ההדפסה

שלושת הכרטיסים של שאלה חייבים לצאת יחד ובאותו סדר שבו יגזרו אותם. בגיליון
ההדפסה כל שאלה מקבלת מפריד קטן משלה:

```
❓ באיזו שנה הוקם היישוב?  ·  3 תשובות
[1948]  [1952]  [1967]
```

המפריד הוא רק בגיליון ההדפסה של המפעיל — הוא לא חלק מהכרטיס הנגזר, וגם
הוא לא מסמן את הנכונה.

## 6. הסריקה

`src/hooks/useScoreSubmit.ts` — כאן עוברת כל סריקה במערכת (הקיוסק, הזנה
ידנית, ומסך ההגרלה), אז כל השינוי יושב במקום אחד.

היום: `actions` לפי `code` → נמצא או "משימה לא נמצאה".
עכשיו, פותר בשני שלבים:

```ts
// 1. משימה רגילה - כמו היום, בלי שינוי
let action = await lookupActionByCode(eventId, aCode)
let option: { id: string; label: string; is_correct: boolean } | null = null

// 2. לא נמצאה? אולי זה קוד תשובה
if (!action) {
  const opt = await lookupOptionByCode(eventId, aCode)   // action_options + actions
  if (opt) { action = opt.action; option = opt }
}
if (!action) return { ok: false, error: `משימה "${aCode}" לא נמצאה.` }
```

השאילתה השנייה רצה **רק** כשהראשונה נכשלה, כך שסריקה רגילה לא משלמת עליה
כלום.

מכאן והלאה הכול זהה: `canPerformAction` מקבל את אותה משימה, אותן מגבלות,
אותה בדיקת קבוצות. שני הבדלים בלבד:

```ts
const isCorrect = option ? option.is_correct : true
const awardedPoints = isCorrect ? action.points : 0
```

- **הטרנזקציה נרשמת תמיד**, גם על תשובה שגויה, עם `points = 0`. זה מה
  שהופך את "ניסיון אחד" לאמיתי: `max_completions = 1` סופר את הסריקה
  השגויה, והכרטיס השני והשלישי נחסמים ב-`LIMIT_REACHED`. בלי זה הפיצ'ר
  חסר משמעות — סורקים עד שמצליחים.
- **`ScoreSubmitResult` גדל** ב-`isCorrect: boolean` וב-`optionLabel:
  string | null`. משימה רגילה מחזירה `true` ו-`null`, וכל קורא קיים
  שמתעלם מהם מתנהג בדיוק כמו היום.

**`check_and_award_rewards`** רץ כרגיל — 0 נקודות לא חוצות שום סף, אז אין
מה להגן עליו.

**`isCorrect: false` לא הופך את התוצאה ל-`ok: false`.** תשובה שגויה היא
סריקה שהצליחה עם 0 נקודות, לא כישלון. ההבחנה הזו היא מה שמאפשר לקיוסק
להראות מסך תשובה במקום טוסט שגיאה.

## 7. ניסיון אחד

`max_completions = 1` נכתב אוטומטית על כל שאלת טריוויה, והבורר ברשימה נעול
עליו. שילוב עם `daily_limit` נחסם במחבר — שאלה יומית היא לא דבר שיש לו
משמעות.

מה שמשתמש רואה כשהוא סורק כרטיס שני של אותה שאלה: ההודעה הקיימת
"המשימה מוגבלת לסריקה אחת - כבר בוצעה", שמגיעה מ-`canPerformAction`. אפשר
לנסח אותה מחדש לטריוויה ("כבר עניתם על השאלה הזו"), וזה שינוי של מחרוזת
אחת שם.

## 8. הקיוסק

`src/pages/EventKioskPage.tsx`

היום `triggerScanSuccess` בונה כרטיס חגיגה אחד. עכשיו הוא מסתעף לפי
`result.isCorrect`:

| | תשובה נכונה | תשובה שגויה |
|---|---|---|
| כותרת | `{שם} · ענה/תה נכון!` | `{שם} · התשובה לא נכונה` |
| שורה שנייה | שם השאלה | שם השאלה + התשובה שנסרקה |
| נקודות | `+20` | `0 נקודות` |
| גוון (`tone`) | הכתום הקיים | אפור-נייטרלי, **לא אדום** |
| צליל | `playScanSuccess()` הקיים | צליל נייטרלי קצר, לא צליל שגיאה |
| אנימציה | החגיגה הקיימת | הופעה שקטה, בלי קונפטי |
| משך | `SCAN_SUCCESS_MS` | אותו משך |

**התשובה הנכונה לא נחשפת על המסך.** מסך שמראה "הנכונה הייתה 1952" הופך את
הקיוסק לדלת אחורית — מי שעומד מאחור בתור רואה אותה ועונה נכון. אם הלקוח
ירצה חשיפה, זו הרחבה מכוונת עם מתג משלה, לא ברירת מחדל.

`popupBusyRef`, תור החגיגות, `holdPrizeReveal` — כולם עובדים כמו שהם.
תשובה שגויה לא זוכה בפרס, אז `celebrationRewards` ריק והשחרור מיידי.

**רשימת הפעילויות בקיוסק** (הפאנל שמונה משימות וניקוד) מציגה שאלת טריוויה
כשורה אחת עם צ׳יפ `❓` והניקוד שלה. שלוש התשובות לא מופיעות שם.

## 9. שאלה לא תקינה

שאלת טריוויה בלי בדיוק תשובה נכונה אחת לא נכללת בהדפסה — `QrCardGenerator`
מסנן אותה (`isPrintableAction`). והדבר היחיד שאסור שיקרה בשקט הוא בדיוק זה,
שמשהו לא יודפס בלי שאף אחד ידע.

**מומש כאזהרה על שורת המשימה עצמה**, לא ברשימת המוכנות: צ׳יפ אדום
"חסרה תשובה נכונה - לא תודפס" ב-`ActionRow`, בדיוק ליד הכפתור שפותח את
המחבר לתקן.

זו סטייה מכוונת מהאפיון המקורי. `calculateReadiness` מקבל רק
`(event, counts, groupType)`, ו-`EventCounts` היא מפה של ספירות טבלאות —
"שאלות שבורות" לא נכנסת לצורה הזו, והוספתה הייתה נוגעת ב-StepCards,
ב-ControlCenter, ב-`isEventReady` ובבדיקות שלהם. האזהרה על השורה יושבת
במקום שבו מתקנים את הבעיה ועולה הרבה פחות.

**איך בכלל מגיעים למצב הזה:** `updateTriviaQuestion` מנקה את כל דגלי
`is_correct` ואז מסמן אחד (בגלל האינדקס הייחודי בסעיף 3). כתיבה שנופלת
באמצע משאירה שאלה בלי תשובה נכונה. זה התרחיש האמיתי, ולא רק עריכה ידנית
במסד.

## 10. הגרלה, SMS, ומכסת ניסיון

**מסך ההגרלה** (`useLotteryScanner.ts`) — סריקה שגויה **לא מזכה בכרטיס
להגרלה**. היום כל `response.ok` מוסיף כרטיס; מוסיפים `&& response.result.isCorrect`
לפני `addScanLotteryEntry`, ומראים `feedback` נייטרלי במקום `scored`.
בלי זה שאלת טריוויה הופכת לדרך להיכנס להגרלה בלי לענות נכון.

**SMS** (`src/lib/scanSms.ts`) — לא נשלחת הודעה על תשובה שגויה. התבנית של
הלקוח בנויה סביב "צברת X נקודות על Y", והודעה שאומרת 0 היא לא מה שהוא
כתב. תנאי אחד בקריאה ב-`useScoreSubmit`: `if (smsNotifications && result.isCorrect)`.

**מכסת סריקות הניסיון** — סריקה שגויה **כן** נספרת במכסה. היא סריקה
לכל דבר מבחינת המערכת, והחריגה תבלבל יותר משתעזור.

## 11. מצב אופליין

`src/lib/offline/`

- `types.ts`: `GamePack` מקבל `actionOptions?: ActionOption[]`, ו-`LocalScan`
  מקבל `actionOptionId`.

  **`PACK_VERSION` נשאר 1** — סטייה מהאפיון. תבנית הנגן נארזת עם האפליקציה
  והחבילה מוזרקת לתוכה בזמן הייצוא (`exportGame.ts`), כך ששתיהן תמיד מאותו
  build ואי אפשר להגיש חבילה חדשה לנגן ישן. `actionOptions` גם אופציונלי:
  חבילה בלי השדה היא חבילה בלי טריוויה. העלאת המספר לא הייתה מגינה על
  כלום והייתה רק מספר שהשתנה.
- `exportGame.ts`: מושך את `action_options` של המשחק לחבילה.
- `scoreEngine.ts`: אותה פתירה דו-שלבית של סעיף 6, מול המערכים שבחבילה
  במקום מול `supabase`. הקובץ כבר מצהיר ש-`useScoreSubmit` הוא מקור האמת
  שלו — השינוי חייב להיות מקבילי ומדויק.
- `fixtures.ts` + `scoreEngine.test.ts`: מקרה טריוויה — נכונה מזכה, שגויה
  נותנת 0 ונרשמת, סריקה שנייה נחסמת.

## 12. ניהול ודוחות

- **`ScansTab`** — שורת סריקה של טריוויה מציגה את השאלה ואת התשובה שנסרקה,
  עם `✓`/`✗` שקט וניקוד 0. היום היא מציגה `actionName` בלבד, ובלי זה נראה
  ברשימה "0 נקודות" בלי שום הסבר.
- **`useEventScans`** — מצרפת את התשובה שנסרקה לכל שורה, בשאילתה **נפרדת**
  ולא ב-join על שאילתת הסריקות. מסד בלי 088 היה מפיל את כל יומן הסריקות
  יחד עם ה-join; לבד, כישלון שלה פירושו "אף סריקה לא נשאה תשובה", וזה
  בדיוק נכון במסד כזה.

  השאילתה לא משתמשת ב-`.not()` בכוונה: ה-shim של הנגן האופליין
  (`src/offline/shim/supabase.ts`) מממש תת-קבוצה של בונה השאילתות, ו-`.not`
  אינו בה — קריאה אליו הייתה זורקת בתוך הקובץ המיוצא. הסינון נעשה בקוד.

  הקישור: `point_transactions.action_option_id` — עמודה שקטה, `NULL` בכל
  סריקה רגילה, חלק ממיגרציה 088. היא נדרשת רק לדיווח; הניקוד לא צריך אותה.
- **דוח המשתתפים** (`useEventParticipantsReport` / הייצוא) — עמודה
  "תשובות נכונות" כשיש טריוויה במשחק. אחרת הדוח מראה משתתף עם 12 סריקות
  ו-200 נקודות ולא מסביר את הפער.
- **מחיקת סריקה** (מיגרציה 074) — עובדת כמו שהיא. מחיקה של סריקה שגויה
  מחזירה למשתתף את הניסיון, וזה בדיוק ההתנהגות הרצויה כשמפעיל מתקן טעות.

## 13. תבניות

`src/lib/templates.ts` · `template_tasks`

תבנית ששומרים ממשחק עם שאלות טריוויה **חייבת** לטפל בהן, אחרת השאלה
מועתקת כמשימה רגילה ששמה הוא השאלה ואין לה תשובות — כלומר שקט ושבור.

מיגרציה 088 מוסיפה גם:

```sql
ALTER TABLE template_tasks ADD COLUMN IF NOT EXISTS kind TEXT NOT NULL DEFAULT 'standard';
CREATE TABLE template_task_options ( ... כמו action_options, בלי code ... );
```

בלי `code` — הקודים נוצרים מחדש בכל משחק שנפתח מהתבנית.

`syncEventToTemplate` ו-`applyTemplateToEvent` מעתיקים את התשובות לשני
הכיוונים. `LockedActionCard` (משימה נעולה בתבנית שלא נרכשה) מציגה שאלת
טריוויה כמו כל משימה אחרת — היא נעולה בכל מקרה.

## 14. מה בכוונה לא בגרסה הראשונה

- **מספר תשובות משתנה.** שלוש, קבוע. הסכימה כבר תומכת ב-N דרך `sort_order`,
  ו-2 או 4 זה שינוי במחבר בלבד — אבל לא עכשיו.
- **ניקוד שלילי על טעות.** 0 קבוע. אם יידרש, זו עמודה `wrong_points` על
  `actions` ושדה אחד במחבר, ושום דבר אחר במסמך הזה לא זז.
- **חשיפת התשובה הנכונה בקיוסק.** סעיף 8 מסביר למה.
- **תמונות בתשובות.** רק טקסט.

## 15. סדר בנייה

| # | מה | למה בסדר הזה |
|---|---|---|
| 1 | ✅ מיגרציה 088 + `APPLY_TRIVIA_TASKS.sql` + `triviaTasksFlag.ts` + טיפוסים | הכול נשען על זה, וכלום עוד לא מוצג |
| 2 | `useScoreSubmit` — פתירת קוד תשובה, `isCorrect`, ניקוד 0 | אפשר לבדוק בהזנה ידנית עם שורות שנכתבו ביד ב-DB |
| 3 | המחבר + `ActionRow` + הכפתור ב-`ActionList` | מכאן הלקוח יכול ליצור שאלה |
| 4 | ההדפסה: `cardCounts`, `QrCardGenerator`, הנוסח ב-`StepCards` | מכאן יש כרטיסים אמיתיים לסרוק |
| 5 | הקיוסק: מסך התשובה השגויה | החוויה בפועל |
| 6 | הגרלה + SMS + `ScansTab` + הדוח | ההשפעות בקצוות |
| 7 | אופליין + תבניות | שני העותקים של המנוע |

צעדים 1–5 הם פיצ'ר עובד. 6–7 הם מה שמונע ממנו להתנהג מוזר בפינות.
