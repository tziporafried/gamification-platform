# סוג הקבוצה — קבוצת תחרות מול קבוצת חלוקה — אפיון

דגל: `group_purpose` · היקף: משחק אחד · מיגרציה: 090.

**סטטוס: ממומש.** מה שנותר הוא להריץ את `supabase/APPLY_GROUP_PURPOSE.sql`
וליצור את שורת הדגל בפאנל האדמין — עד אז הכל כבוי ושום התנהגות קיימת לא
השתנתה.

---

## 1. מה זה

קבוצה במערכת משמשת היום לשני דברים שונים לגמרי, והאפליקציה לא יודעת להבדיל
ביניהם:

* **צד בתחרות** — יש לה ניקוד, היא מדורגת מול הקבוצות האחרות, היא יכולה לנצח.
* **דרך לפנות לחלק מהמשתתפים** — המשימה הזו לחניכים, הפרס הזה לצוות, ההגרלה
  הזו רק בין האורחים.

השני נבנה מתוך הראשון, כי הראשון היה כל מה שיש. המחיר מופיע בטבלת הדירוג:
"צוות היגוי", קבוצה שנוצרה רק כדי לכוון אליה שלוש משימות, מופיעה על הפודיום
בסוף המשחק לצד הקבוצות שבאמת התחרו — בדרך כלל בתחתית, כי יש בה ארבעה אנשים.
הפתרון של המנהלת היום הוא פשוט לא ליצור את הקבוצה, ולחלק את המשימות ביד.

עם הדגל, לכל קבוצה יש **תפקיד**:

| | קבוצת תחרות | קבוצת חלוקה |
|---|---|---|
| טבלת דירוג ופודיום | כן | **לא** |
| שיוך משימות | כן | כן |
| שיוך פרסים | כן | כן |
| סינון הגרלה | כן | כן |
| שיוך משתתפים | כן | כן |

זה השינוי כולו. בכל מקום שבו **בוחרים** קבוצה — משימה, פרס, הגרלה, שיוך
משתתף — שני הסוגים מוצעים ומתנהגים אותו דבר. רק הדירוג משתנה.

## 2. הדגל

```
key           group_purpose
label         סוגי קבוצות
description   הבחנה בין קבוצת תחרות (מדורגת) לקבוצת חלוקה (משימות, פרסים והגרלות בלבד).
default_plans (ריק - נמכר לפי משחק)
```

נוצר **ביד בפאנל האדמין**, לא במיגרציה.

```ts
// src/lib/groups/groupPurposeFlag.ts
export const GROUP_PURPOSE_FLAG = 'group_purpose'
export function useGroupPurpose(): boolean {
  return useFeatureFlag(GROUP_PURPOSE_FLAG)
}
```

**הדגל שולט על הבחירה, לא על הדירוג.** הבורר בטופס הקבוצה, התווית על
הכרטיס, והנכונות של הויזארד להשאיר קבוצות במשחק אישי — כולם מאחורי הדגל.
`get_group_leaderboard` מסננת קבוצות חלוקה לכולם, כי SQL לא יכולה לקרוא את
קטלוג הדגלים — וגם לא צריך: בלי הדגל אף לקוח לא כותב `'distribution'`, ואין
מה לסנן. המשמעות: משחק שהדגל הוסר ממנו אחרי שכבר סידר את הקבוצות שלו נשאר
מסודר. המנהלת כבר לא יכולה לשנות תפקיד, והדירוג ממשיך לכבד את מה שנקבע —
להחזיר ארבע קבוצות חלוקה לפודיום באמצע משחק ייראה לכולם באולם כמו באג.

## 3. מודל הנתונים — מיגרציה 090

```sql
ALTER TABLE groups
  ADD COLUMN IF NOT EXISTS purpose TEXT NOT NULL DEFAULT 'competition';
  -- CHECK (purpose IN ('competition', 'distribution'))
```

כל שורה קיימת היא `competition`, כלומר בדיוק מה שהיא הייתה. אין backfill, אף
שורה לא נכתבת מחדש, ואף שאילתה קיימת לא משתנה.

`get_group_leaderboard` מוחלפת (`CREATE OR REPLACE`) והתוספת היא שורה אחת:

```sql
WHERE g.event_id = v_event_id
  AND g.purpose <> 'distribution'
```

נכתב `<> 'distribution'` ולא `= 'competition'` בכוונה: תפקיד שיתווסף בעתיד
ידורג עד שיוגדר אחרת. קבוצה שנעלמת מהטבלה נראית כמו נקודות שאבדו, וזה גרוע
משורה אחת מיותרת.

**נקודות לא נגעו בהן.** משתתף בקבוצת חלוקה עדיין צובר לעצמו, ועדיין צובר
לכל קבוצת תחרות שהוא שייך אליה. `point_transactions` לא יודעת שקבוצות קיימות.

## 4. איפה זה בקוד

| קובץ | מה |
|---|---|
| `supabase/migrations/090_group_purpose.sql` | העמודה + ה-RPC |
| `supabase/APPLY_GROUP_PURPOSE.sql` | אותו דבר, להדבקה ב-SQL editor |
| `src/lib/groups/groupPurpose.ts` | קריאה טהורה: `groupPurpose`, `isCompetingGroup`, תוויות. מיובא גם ע"י הנגן האופליין — אסור לו לגעת ב-React |
| `src/lib/groups/groupPurposeFlag.ts` | הדגל |
| `src/lib/offline/leaderboard.ts` | אותו סינון, בנגן האופליין |
| `src/components/groups/GroupCard.tsx` | תווית התפקיד, ולחיצה עליה מחליפה |
| `src/components/groups/GroupForm.tsx` | בורר התפקיד ביצירה ובעריכה |
| `src/components/groups/GroupSelectDropdown.tsx` | סימון קטן לקבוצת חלוקה ברשימת הבחירה |
| `src/components/wizard/StepGroups.tsx` | ראו סעיף 5 |
| `src/components/participants/ParticipantList.tsx` | שיוך משתתפים גם במשחק אישי; משתתף חדש נכנס לקבוצות התחרות בלבד |
| `src/components/actions/ActionList.tsx` | משימות מוצעות לקבוצות גם במשחק אישי |
| `LeaderboardSection` / `WinnersCeremony` | תג הקבוצה של משתתף מוגבל לקבוצות שהטבלה מדרגת |

## 5. הויזארד — שלב הקבוצות

השאלה בשלב 2 ("תחרות בין קבוצות" / "תחרות בין משתתפים") נשארת, והיא עכשיו
שאלה על **קבוצות התחרות בלבד**. שני שינויים:

1. **"תחרות בין משתתפים" כבר לא מוחקת הכל.** היא מוחקת את קבוצות התחרות
   ומשאירה את קבוצות החלוקה. חלון האישור אומר בדיוק את זה ומונה כל סוג בנפרד.
   בלי הדגל הטקסט וההתנהגות זהים למה שהיו.
2. **משחק אישי יכול להוסיף קבוצות.** כשהדגל דלוק ונבחרה תחרות בין משתתפים,
   השלב ממשיך להציג את רשימת הקבוצות, כל קבוצה שנוצרת בו היא קבוצת חלוקה,
   ומשפט אחד מסביר למה בכלל מבקשים קבוצות בתחרות אישית. אפשר גם פשוט להמשיך
   הלאה בלי להוסיף אף אחת.

המעבר לשלב הבא מותנה בקבוצת תחרות אחת לפחות — בדיוק כמו קודם, רק שעכשיו
נספרות רק קבוצות התחרות. בתחרות בין משתתפים אין תנאי, כמו קודם.

## 6. מה נשאר בחוץ

* **ייבוא רוסטר מקובץ** יוצר קבוצות תחרות, כמו תמיד. קובץ לא אומר תפקיד, ואפשר
  להפוך כל קבוצה לקבוצת חלוקה בלחיצה על הכרטיס שלה.
* **מגבלות התוכנית** סופרות קבוצה כקבוצה. קבוצת חלוקה תופסת מקום במכסה בדיוק
  כמו כל אחת אחרת.
* **תבניות פעילות** מייצרות קבוצות תחרות. אין היום תבנית שמבקשת אחרת.
